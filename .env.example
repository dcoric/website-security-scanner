# Security Scan Configuration

# Target Website
TARGET_URL=https://example.com

# URL Prefixes to Skip (comma-separated)
# Example: /blog,/news
SKIP_URL_PREFIXES=

# URL Prefixes to Include (overrides skip)
# Example: /cities/boston (even if /cities is skipped)
INCLUDE_URL_PREFIXES=

# SMTP (Email) Configuration
# Example for Gmail: smtp.gmail.com, 587, and an App Password
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=secret_password
EMAIL_FROM=scanner@example.com
EMAIL_TO=admin@example.com

# LLM Configuration (Optional)
# Providers: openai, deepseek, gemini, custom
# Recommended Models:
#   openai: gpt-4o, gpt-4-turbo
#   deepseek: deepseek-chat, deepseek-coder
#   gemini: gemini-1.5-pro, gemini-1.5-flash
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=
# LLM_BASE_URL=https://api.deepseek.com # Only needed for 'custom' or overriding defaults

# Google Safe Browsing API Key (Optional but recommended for malware checks)
GOOGLE_SAFE_BROWSING_KEY=
